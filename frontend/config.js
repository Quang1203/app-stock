/**
 * VN Stock Monitor – Backend Configuration
 * 
 * Development:   Leave BACKEND_URL empty → auto-uses localhost:8000
 * Production:    Set BACKEND_URL to your Railway/Render URL (no trailing slash)
 *
 * Example:
 *   window.APP_CONFIG.BACKEND_URL = 'https://vn-stock-monitor.up.railway.app';
 */
window.APP_CONFIG = {
  BACKEND_URL: '',   // e.g. 'https://your-backend.up.railway.app'
};
